import React from "react";
import { Layout } from "@/components/ui";
import { DataTable } from "@/components/DataTable";

const Home = () => (
  <div className="App">
    <Layout>
      <DataTable />
    </Layout>
  </div>
);

export default Home;
